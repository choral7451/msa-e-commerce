server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git: #default
          uri: https://github.com/choral7451/msa-e-commerce.git
          default-label: ma`in